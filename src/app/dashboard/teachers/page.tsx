import { PlaceholderPage } from "@/components/common/placeholder-page";

export default function TeachersPage() {
  return <PlaceholderPage title="Teachers" description="Teacher management page is available and can now be extended with CRUD UI." />;
}
